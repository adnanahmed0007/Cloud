services:
  frontend:
    build: "./cloudex-frontend (1)"
    ports:
      - "5173:5173"

  backend:
    build: ./Server
    ports:
      - "9090:9090"
    env_file:
      - ./Server/.env
    depends_on:
      - redis

  redis:
    image: redis:latest
    ports:
      - "6379:6379"
