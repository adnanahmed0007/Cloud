info:
  name: upload
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:9090/auth/api/signup
  body:
    type: json
    data: |-
      {
         
        "email": "adnan@gmail.com",
        "password": "Adnan@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
