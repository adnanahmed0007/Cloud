info:
  name: Signup
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9090/auth/api/login
  body:
    type: json
    data: |-
      {
         
        "email": "adnan@gmail.com",
        "password": "Adnan@123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
